PLACEHOLDER ONLY — do not put a real credential in this file.

Copy this to ./secrets/now-sdk-credential (no extension) and fill in
real values there. That path is gitignored and must never be committed —
see CLAUDE.md's secrets rule.

Exact shape (basic-auth vs token) will be confirmed in NOW-9 against
now-sdk's actual auth requirements; treat the JSON below as illustrative
until then.

{
  "instanceUrl": "https://your-instance.service-now.com",
  "username": "REPLACE_ME",
  "password": "REPLACE_ME"
}
