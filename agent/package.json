{
  "name": "nowagent-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Docker-hosted agent container for the Fluent SDK Agent project. Container shell only at this stage — see NOW-8.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
